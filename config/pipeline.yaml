datasets:
  brats2020:
    root: /mnt/disk1/datasets/BraTS-2020
    enabled: true
  rhuh_gbm:
    root: /mnt/disk1/datasets/RHUH-GBM
    enabled: true
  upenn_gbm:
    root: /mnt/disk1/datasets/UPENN-GBM
    enabled: true
  ucsf_pdgm:
    root: /mnt/disk1/datasets/UCSF-PDGM
    enabled: true

output_dir: /home/humanailab3/Project/gbm-surv-data-pipeline/output

workers: 8

dedup:
  demographic_age_tol: 1.0
  demographic_days_tol: 5.0
  close_age_tol: 0.5
  close_days_tol: 2.0

# Which study the cohort stage emits. Eligibility rules, survival thresholds,
# the held-out cohort and duplicate priority are properties of the study itself
# and live in gbm_os/studies.py — run `gbm-manifest studies` to list them.
cohort:
  study: gbm-os
